Write a test for the `create_task` function in `tasks.py`.

The function creates a task dictionary with an ID, title, description, priority, status,
and timestamp. Test that it works correctly with default arguments and with custom values.
